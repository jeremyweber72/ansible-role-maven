---
maven_version: 3.5.4
maven_url:
  http://www.apache.org/dist/maven/maven-3/{{ maven_version }}/binaries/apache-maven-{{ maven_version }}-bin.zip
maven_checksum_url:
  http://www.apache.org/dist/maven/maven-3/{{ maven_version }}/binaries/apache-maven-{{ maven_version }}-bin.zip.sha512
maven_checksum: "md5: {{ lookup('url', maven_checksum_url) }}"
maven_prefix: /usr/local
maven_dir: "{{ maven_prefix }}/apache-maven-{{ maven_version }}"
